<Page x:Class="ApartmentChain.Pages.MainPageAdmin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ApartmentChain.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Страница администратора" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid Margin="10" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Пользователи" Grid.Row="0" FontWeight="Bold" Style="{StaticResource TextBlockStyle}"/>
        <DataGrid x:Name="UsersDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" HorizontalContentAlignment="Center">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" MinWidth="30" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="Имя" MinWidth="100" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Фамилия" MinWidth="100" Binding="{Binding Surename}"/>
                <DataGridTextColumn Header="Дата рождения" MinWidth="100" Binding="{Binding Birthday, StringFormat=dd.MM.yyyy}"/>
                <DataGridTextColumn Header="Номер телефона" MinWidth="120" Binding="{Binding PhoneNumber}"/>
                <DataGridTextColumn Header="Роль" MinWidth="100" Binding="{Binding Roles.Role}"/>
                <DataGridTextColumn Header="Логин" MinWidth="80" Binding="{Binding Login}"/>
                <DataGridTextColumn Header="Пароль" MinWidth="80" Binding="{Binding PasswordHash}"/>
                <DataGridTemplateColumn Header="Действия" MinWidth="300">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Редактировать" Click="EditUser_Click" Height="30" Width="140" Style="{StaticResource ButtonStyle}"/>
                                <Rectangle Width="10"/>
                                <Button Content="Удалить" Click="DeleteUser_Click" Height="30" Width="140" Style="{StaticResource ButtonStyle}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Text="Бронирования" Grid.Row="2" FontWeight="Bold" Style="{StaticResource TextBlockStyle}"/>
        <DataGrid x:Name="BookingsDataGrid" Grid.Row="3" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ID}"/>
                <DataGridTextColumn Header="Апартаменты" Binding="{Binding Apartaments.ApartmentName.Name}"/>
                <DataGridTextColumn Header="Заезд" Binding="{Binding Arrival}"/>
                <DataGridTextColumn Header="Выезд" Binding="{Binding Departure}"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding Users.Surename}"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding Users.Name}"/>
                <DataGridTextColumn Header="Количество людей" Binding="{Binding AmountOfPeople}"/>
                <DataGridTextColumn Header="Сумма к оплате" Binding="{Binding TotalCost}"/>
                <DataGridTextColumn Header="Статус бронирования" Binding="{Binding BookingStatus.Status}"/>

                <DataGridTemplateColumn Header="Действия" MinWidth="300">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Редактировать" Height="30" Width="140" Style="{StaticResource ButtonStyle}" Click="EditBooking_Click"/>
                                <Rectangle Width="10"/>
                                <Button Content="Удалить" Height="30" Width="140" Style="{StaticResource ButtonStyle}" Click="DeleteBooking_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>