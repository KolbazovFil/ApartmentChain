<Page x:Class="ApartmentChain.Pages.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ApartmentChain.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Главная страница" IsVisibleChanged="Page_IsVisibleChanged">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="205"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel x:Name="SortStackPanel" Orientation="Vertical" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top">
            <TextBlock Text="Фильтры" Style="{StaticResource TextBlockStyle}" HorizontalAlignment="Center" FontWeight="Bold"/>
            <TextBlock Text="Минимальная цена" Margin="0,10,0,0" Style="{StaticResource TextBlockStyle}" />
            <TextBox Name="MinPriceTextBox" ToolTip="Введите минимальную цену для фильтрации" TextChanged="FilterTextBox_TextChanged" Style="{StaticResource TextBoxStyle}" Width="200" Height="30"/>
            <TextBlock Text="Максимальная цена" Margin="0,10,0,0" Style="{StaticResource TextBlockStyle}"/>
            <TextBox Name="MaxPriceTextBox" ToolTip="Введите максимальную цену для фильтрации" TextChanged="FilterTextBox_TextChanged" Style="{StaticResource TextBoxStyle}" Width="200" Height="30"/>
            <TextBlock Text="Город" Margin="0,10,0,0" Style="{StaticResource TextBlockStyle}"/>
            <TextBox Name="CityTextBox" ToolTip="Введите название города" Style="{StaticResource TextBoxStyle}" Width="200" Height="30" TextChanged="CityTextBox_TextChanged"/>
            <Button Name="CleanButton" Content="Очистить" ToolTip="Нжмите чтобы очистить фильтр" Height="40" Margin="0,10,0,0" Style="{StaticResource ButtonStyle}" Click="CleanButton_Click"/>
        </StackPanel>

        <ListView Name="ApartList" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Column="1" BorderThickness="0" Background="Transparent">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="450"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>

                        <Border DataContext="{Binding PhotosViewModel}" Grid.Row="0" HorizontalAlignment="Center" Width="500" Margin="5">
                            <Grid>
                                <Image Source="{Binding CurrentPhoto}" Stretch="Fill"/>
                                <Button x:Name="PrevPhotoButton" Content="◀" Command="{Binding PrevPhotoCommand}" HorizontalAlignment="Left" Style="{StaticResource CommandButtonStyle}"/>
                                <Button x:Name="NextPhotoButton" Content="▶" Command="{Binding NextPhotoCommand}" HorizontalAlignment="Right" Style="{StaticResource CommandButtonStyle}"/>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Row="0" HorizontalAlignment="Left" VerticalAlignment="Top" Orientation="Horizontal" Height="30" Width="500">
                            <Rectangle Width="5"/>
                            <TextBlock Text="{Binding Cities}" Style="{StaticResource TextBlockStyle}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Address}" Style="{StaticResource TextBlockStyle}" FontWeight="Bold"/>
                            <TextBlock Text="{Binding Building}" Style="{StaticResource TextBlockStyle}" FontWeight="Bold"/>
                        </StackPanel>

                        <Button Name="ToBookButton" Content="Забронировать" ToolTip="Перейти к бронированию" Grid.Row="2" Margin="0,10,0,0" HorizontalAlignment="Left" Tag="{Binding Id}" Style="{StaticResource ButtonStyle}" Click="ToBookButton_Click"/>
                        <TextBlock Text="{Binding Price, StringFormat=Цена: {0} ₽ в сутки}" Grid.Row="2" HorizontalAlignment="Right" Margin="0,10,0,0" Style="{StaticResource TextBlockStyle}" FontWeight="Bold"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</Page>